# 个股明细分组优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将面板"个股明细"改为按股票名称拼音首字母分组（A-Z + #），提供字母索引条、手风琴展开/收起与搜索框，解决股票多时难以翻阅的问题。

**Architecture:** 后端（Python）用 pypinyin 为每只股票计算首字母字段 `initial`（A-Z/#）与完整拼音首字母串 `pinyin_initials`（用于"ywk"式搜索），在 `stock_to_dict` 序列化时输出；前端（dashboard.py 内嵌 HTML/CSS/JS）按 `initial` 分组渲染，实现索引条、手风琴与搜索过滤。数据抓取、灯号计算、CLI/Markdown/CSV 均不动。

**Tech Stack:** Python 3.12（.venv）、pypinyin、pytest（仅开发用）、原生 HTML/CSS/JS（无框架）。

## Global Constraints

- 只改：`lightmon/pinyin_util.py`（新建）、`lightmon/models.py`、`dashboard.py`、`requirements.txt`、`tests/`（新建）
- 不动：抓数逻辑、灯号计算、`main.py`、`lightmon/reporter.py`、CLI/Markdown/CSV 输出
- 面板其他区块（今日行情、灯号总览、自选股管理）保持现状
- 分组规则：中文名按 pypinyin 默认读音取首字母大写（A-Z）；名称为空或首字符非汉字 → "#"
- 搜索支持：中文名、代码、拼音首字母串（如 `ywk`），大小写不敏感，150ms 防抖
- 默认全部收起；展开/收起动画 250ms；尊重 `prefers-reduced-motion`
- 完成后必须提交 git
- 测试用 pytest，安装到 `.venv`（不写入 requirements.txt 运行时依赖，仅新增 `pypinyin`）

---

### Task 1: 拼音首字母工具（TDD）

**Files:**
- Modify: `requirements.txt`（追加 `pypinyin`）
- Create: `lightmon/pinyin_util.py`
- Test: `tests/test_pinyin_util.py`

**Interfaces:**
- Consumes: 无
- Produces:
  - `initial_of_name(name: Optional[str]) -> str` — 返回 A-Z 或 "#"
  - `initials_of_name(name: Optional[str]) -> str` — 返回大写拼音首字母串（如 "YWK"），空串为 ""

- [ ] **Step 1: 安装依赖**

```powershell
& '.\.venv\Scripts\python.exe' -m pip install pypinyin pytest
```

并在 `requirements.txt` 末尾追加一行：

```text
pypinyin>=0.51.0
```

- [ ] **Step 2: 创建测试目录与失败测试**

创建 `tests/test_pinyin_util.py`：

```python
# -*- coding: utf-8 -*-
from lightmon.pinyin_util import initial_of_name, initials_of_name


def test_initial_basic_names():
    assert initial_of_name("英维克") == "Y"
    assert initial_of_name("金富科技") == "J"
    assert initial_of_name("飞荣达") == "F"
    assert initial_of_name("申菱环境") == "S"
    assert initial_of_name("天地科技") == "T"


def test_initial_polyphone_default():
    assert initial_of_name("中航光电") == "Z"
    assert initial_of_name("中国平安") == "Z"


def test_initial_non_chinese_falls_to_hash():
    assert initial_of_name("3M中国") == "#"
    assert initial_of_name("600519") == "#"
    assert initial_of_name("") == "#"


def test_initials_full_string():
    assert initials_of_name("英维克") == "YWK"
    assert initials_of_name("贵州茅台") == "GZMT"
    assert initials_of_name("") == ""
```

- [ ] **Step 3: 运行测试，确认失败**

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/test_pinyin_util.py -v
```

预期：`ModuleNotFoundError: No module named 'lightmon.pinyin_util'`。

- [ ] **Step 4: 实现 `lightmon/pinyin_util.py`**

```python
# -*- coding: utf-8 -*-
"""股票名称 -> 拼音首字母。"""
from typing import Optional

try:
    from pypinyin import Style, lazy_pinyin
except ImportError:  # 依赖缺失时降级：返回空串，前端归入 "#" 组
    lazy_pinyin = None


def _is_cjk(ch: str) -> bool:
    return "\u4e00" <= ch <= "\u9fff"


def initials_of_name(name: Optional[str]) -> str:
    """返回名称完整拼音首字母串（大写），如 '英维克' -> 'YWK'。"""
    if not name or lazy_pinyin is None:
        return ""
    chars = lazy_pinyin(str(name), style=Style.FIRST_LETTER)
    return "".join(ch[0] for ch in chars if ch).upper()


def initial_of_name(name: Optional[str]) -> str:
    """返回名称首字母（A-Z）；名称为空或首字符非汉字返回 '#'。"""
    text = str(name or "")
    if not text or not _is_cjk(text[0]):
        return "#"
    initials = initials_of_name(text)
    return initials[0].upper() if initials else "#"
```

- [ ] **Step 5: 运行测试，确认通过**

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/test_pinyin_util.py -v
```

预期：4 passed。

- [ ] **Step 6: 提交**

```bash
git add requirements.txt lightmon/pinyin_util.py tests/test_pinyin_util.py
git commit -m "feat: 拼音首字母工具（pypinyin）"
```

---

### Task 2: 序列化输出增加 initial 字段（TDD）

**Files:**
- Modify: `lightmon/models.py`（`stock_to_dict` 增加两个键）
- Test: `tests/test_models_initial.py`

**Interfaces:**
- Consumes: `initial_of_name`、`initials_of_name`（Task 1）
- Produces: `stock_to_dict(stock)` 返回的字典新增 `initial: str` 与 `pinyin_initials: str`

- [ ] **Step 1: 写失败测试**

创建 `tests/test_models_initial.py`：

```python
# -*- coding: utf-8 -*-
from lightmon.models import StockSnapshot, stock_to_dict


def test_stock_to_dict_includes_initial():
    stock = StockSnapshot(code="002837", name="英维克")
    data = stock_to_dict(stock)
    assert data["initial"] == "Y"
    assert data["pinyin_initials"] == "YWK"


def test_stock_to_dict_initial_hash_for_empty_name():
    stock = StockSnapshot(code="600519", name="")
    data = stock_to_dict(stock)
    assert data["initial"] == "#"
```

- [ ] **Step 2: 运行测试，确认失败**

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/test_models_initial.py -v
```

预期：`KeyError: 'initial'`。

- [ ] **Step 3: 修改 `lightmon/models.py`**

在文件顶部增加 import：

```python
from .pinyin_util import initial_of_name, initials_of_name
```

在 `stock_to_dict` 的 `keys` 列表中 `"industry_light"` 之前加入：

```python
        "initial", "pinyin_initials",
```

在 `return {key: getattr(stock, key) for key in keys}` 改为：

```python
    data = {key: getattr(stock, key) for key in keys}
    data["initial"] = initial_of_name(stock.name)
    data["pinyin_initials"] = initials_of_name(stock.name)
    return data
```

- [ ] **Step 4: 运行测试，确认通过**

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/ -v
```

预期：全部通过（Task 1 + Task 2 共 6 个测试）。

- [ ] **Step 5: 提交**

```bash
git add lightmon/models.py tests/test_models_initial.py
git commit -m "feat: 序列化增加拼音首字母字段"
```

---

### Task 3: 前端改造 —— 搜索框 + 字母索引条 + 手风琴分组

**Files:**
- Modify: `dashboard.py`（仅 PAGE 字符串内：CSS、个股明细 HTML、JS）

**Interfaces:**
- Consumes: `/api/status` 返回的每只股票新增 `initial`、`pinyin_initials` 字段（Task 2）
- Produces: 面板"个股明细"区块的新渲染逻辑

- [ ] **Step 1: 替换"个股明细"HTML**

在 `dashboard.py` 的 `PAGE` 字符串中，将现有个股明细 section 整体替换为：

```html
  <section class="panel">
    <h2>个股明细<span class="hint">按拼音首字母分组 · 点击组头展开 · 支持搜索</span></h2>
    <div class="stock-tools">
      <input id="stockSearch" type="search" placeholder="搜索名称 / 代码 / 拼音首字母（如 ywk）">
      <div id="letterBar" class="letter-bar"></div>
    </div>
    <div id="stockGroups" class="groups"></div>
    <div id="groupEmpty" class="empty hidden">没有匹配的股票</div>
  </section>
```

- [ ] **Step 2: 追加 CSS**

在 `PAGE` 的 `<style>` 中、`@media (max-width:640px)` 之前追加：

```css
.dim{color:var(--muted)}
.stock-tools{position:sticky;top:52px;z-index:5;background:var(--card);padding:10px 0;border-bottom:1px solid var(--line);margin-bottom:10px}
#stockSearch{width:100%;padding:9px 12px;border:1px solid var(--line);border-radius:8px;font:inherit;color:var(--ink);background:#fff}
#stockSearch:focus{outline:none;border-color:#9CA3AF;box-shadow:0 0 0 2px #E5E7EB}
.letter-bar{display:flex;flex-wrap:wrap;gap:3px;margin-top:8px}
.letter-bar button{border:1px solid transparent;background:none;border-radius:6px;padding:2px 7px;font-size:12px;color:var(--muted);cursor:pointer;min-width:24px}
.letter-bar button:hover{background:#F3F4F6;color:var(--ink)}
.letter-bar button.active{background:var(--ink);color:#fff}
.group{margin-bottom:10px;border:1px solid var(--line);border-radius:10px;background:var(--card);overflow:hidden}
.group-head{display:flex;align-items:center;gap:10px;width:100%;padding:10px 14px;background:none;border:none;cursor:pointer;font:inherit;text-align:left}
.group-head:hover{background:#FAFAFB}
.group-head .gl{font-size:16px;font-weight:700;color:var(--ink);width:22px}
.group-head .gc{color:var(--muted);font-size:12px;flex:1}
.group-head .arrow{transition:transform .25s ease;color:var(--muted)}
.group.open .group-head .arrow{transform:rotate(90deg)}
.group-body{display:grid;grid-template-rows:0fr;transition:grid-template-rows .25s ease}
.group.open .group-body{grid-template-rows:1fr}
.group-body-inner{overflow:hidden;min-height:0}
.group-body-inner .cards{padding:12px}
.hl{background:#FDE68A;border-radius:2px;padding:0 1px}
.empty{text-align:center;color:var(--muted);padding:30px 0;font-size:13px}
@media (prefers-reduced-motion:reduce){.group-head .arrow,.group-body{transition:none}}
```

- [ ] **Step 3: 追加 JS**

在 `<script>` 内、`const $ = id => ...` 之后追加：

```js
const LETTERS = "ABCDEFGHIJKLMNOPQRSTUVWXYZ#".split("");
let stockList = [];
let curQuery = "";
let groupState = {};
let spy = null;

function hl(text, q){
  if (!q || text == null) return esc(text);
  const s = String(text);
  const idx = s.toLowerCase().indexOf(q.toLowerCase());
  if (idx < 0) return esc(s);
  return esc(s.slice(0, idx)) + '<span class="hl">' + esc(s.slice(idx, idx + q.length)) + '</span>' + esc(s.slice(idx + q.length));
}

function renderStocks(stocks){
  stockList = stocks || [];
  const q = curQuery.trim().toLowerCase();
  const list = q ? stockList.filter(s =>
    String(s.name || "").toLowerCase().includes(q) ||
    String(s.code || "").includes(q) ||
    String(s.pinyin_initials || "").toLowerCase().includes(q)
  ) : stockList;
  const groups = {};
  list.forEach(s => {
    const L = (s.initial || "#").toUpperCase();
    (groups[L] = groups[L] || []).push(s);
  });
  const letters = Object.keys(groups).sort((a, b) =>
    a === "#" ? 1 : (b === "#" ? -1 : a.localeCompare(b)));

  $("letterBar").innerHTML = LETTERS.map(L =>
    `<button data-letter="${L}" class="${letters.includes(L) ? "" : "dim"}">${L}</button>`).join("");
  Array.from($("letterBar").children).forEach(btn => {
    btn.addEventListener("click", () => jumpTo(btn.dataset.letter));
  });

  $("stockGroups").innerHTML = letters.map(L => {
    const open = q ? true : !!groupState[L];
    return `<div class="group ${open ? "open" : ""}" id="group-${L}" data-letter="${L}">
      <button class="group-head">
        <span class="gl">${L}</span><span class="gc">${groups[L].length} 只</span><span class="arrow">▶</span>
      </button>
      <div class="group-body"><div class="group-body-inner"><div class="cards">${groups[L].map(x => cardHtml(x)).join("")}</div></div></div>
    </div>`;
  }).join("");
  Array.from($("stockGroups").querySelectorAll(".group-head")).forEach(head => {
    head.addEventListener("click", () => {
      const L = head.closest(".group").dataset.letter;
      groupState[L] = !groupState[L];
      renderStocks(stockList);
    });
  });
  $("groupEmpty").classList.toggle("hidden", list.length > 0);
  setupSpy();
}

function jumpTo(L){
  const el = document.getElementById("group-" + L);
  if (el) el.scrollIntoView({behavior: "smooth", block: "start"});
}

function setActiveLetter(L){
  Array.from($("letterBar").children).forEach(b =>
    b.classList.toggle("active", b.dataset.letter === L));
}

function setupSpy(){
  if (spy) spy.disconnect();
  const els = Array.from(document.querySelectorAll(".group"));
  if (!els.length) return;
  spy = new IntersectionObserver(entries => {
    entries.forEach(en => { if (en.isIntersecting) setActiveLetter(en.target.dataset.letter); });
  }, {rootMargin: "-15% 0px -70% 0px"});
  els.forEach(el => spy.observe(el));
}

let searchTimer = null;
document.addEventListener("input", e => {
  if (e.target && e.target.id === "stockSearch"){
    clearTimeout(searchTimer);
    searchTimer = setTimeout(() => { curQuery = e.target.value; renderStocks(stockList); }, 150);
  }
});
```

- [ ] **Step 4: `render()` 改走分组渲染**

在 `render(s)` 函数中，把：

```js
  $("cards").innerHTML = stocks.map(cardHtml).join("");
```

替换为：

```js
  renderStocks(stocks);
```

并把 `cardHtml(x)` 中名称与代码的高亮改为使用 `hl()`：

```js
        <div class="who">${hl(x.name, curQuery)}<span class="cd">${hl(x.code, curQuery)}</span></div>
```

（原为 `esc(x.name)` / `esc(x.code)`）

- [ ] **Step 5: 语法检查 + 重启面板 + 验证分组渲染**

```powershell
& '.\.venv\Scripts\python.exe' -X utf8 -m py_compile dashboard.py
```

重启面板（先释放 8765 端口再启动），等自动分析完成（约 1~2 分钟）后，用 Edge 无头导出 DOM 验证分组：

```powershell
$dom = & 'C:\Program Files (x86)\Microsoft\Edge\Application\msedge.exe' --headless --disable-gpu --virtual-time-budget=8000 --dump-dom 'http://127.0.0.1:8765/' 2>$null
$dom | Select-String 'id="group-Y"|id="group-J"|id="group-F"|id="group-S"|id="group-T"|letter-bar'
```

预期：能看到 `group-Y`、`group-J`、`group-F`、`group-S`、`group-T` 与 `letter-bar`。

- [ ] **Step 6: 交互项人工验证（用户）**

浏览器打开面板确认：组头点击展开/收起、箭头旋转、索引条点击跳转、滚动高亮、搜索（"英维克"/"002837"/"ywk"）、无结果空状态。全部正常后进入 Task 4。

- [ ] **Step 7: 提交**

```bash
git add dashboard.py
git commit -m "feat: 个股明细按拼音首字母分组，支持搜索与字母索引"
```

---

### Task 4: 端到端验证与最终备份

**Files:** 无新文件

- [ ] **Step 1: 完整回归**

```powershell
& '.\.venv\Scripts\python.exe' -m pytest tests/ -v
& '.\.venv\Scripts\python.exe' -X utf8 main.py --codes 002837,300602 --no-report
```

预期：全部测试通过；CLI 正常运行、无北向字段、输出无异常。

- [ ] **Step 2: 面板回归**

确认面板重新加载后：今日行情、灯号总览、个股明细（分组）、建仓清单、需要关注、自选股管理 六个区块全部正常显示。

- [ ] **Step 3: 最终提交**

```bash
git add -A
git commit -m "完成：个股明细分组优化（索引+搜索+手风琴）"
git log --oneline -5
```

预期：出现"完成：个股明细分组优化"提交，历史包含设计文档、计划、各任务提交。
